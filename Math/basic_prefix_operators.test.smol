/*
Description: Validate basic math operations with prefix operators

Steps:
- Run
- Expect global a to be number 8
- Expect global b to be number 7
*/

var a = 10;
var b = --a;

// debugger; // We can use this to break the first Run step, allowing us to check intermediate results. This only works for smol, not Node vm

--a;
--b;
--b;